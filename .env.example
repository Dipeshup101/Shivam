# Derma Analyzer Application Environment Configuration

# Server Configuration
PORT=5000
NODE_ENV=development # Use 'development' or 'production'

# Email Configuration
# For Gmail: use smtp.gmail.com, port 587, secure=false
# For Office 365: use smtp.office365.com, port 587, secure=false
# For custom SMTP: use your provider's settings
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_SECURE=false # Use 'true' for port 465, 'false' for other ports
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password

# Note for Gmail users:
# 1. You may need to enable "Less secure app access" in your Google account
# 2. If you have 2-factor authentication enabled, you'll need to generate an app password:
#    https://myaccount.google.com/apppasswords

# Frontend Configuration
# If you're deploying to production, change this to your actual domain
FRONTEND_URL=http://localhost:3000

# Debug settings
DEBUG_EMAIL=true # Set to 'true' to enable additional email logging 